services:
  backend:
    build:
      context: ./backend/backend
    container_name: dspace-demo-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    container_name: dspace-demo-frontend
    ports:
      - "4200:80"
    depends_on:
      - backend